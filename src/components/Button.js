import styled from 'styled-components'

const Button = styled.button `
    border-radius: 2px;
    cursor: pointer;
    font-size: 14px;
    padding: 5px 10px;
    margin: 0 5px;

    i {
        margin-left: 3px;
    }
`

export default Button